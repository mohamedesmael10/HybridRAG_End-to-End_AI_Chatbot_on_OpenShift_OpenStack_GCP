server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location ~* \.(?:css|js|jpg|jpeg|png|svg|ico|json)$ {
    expires 7d;
    add_header Cache-Control "public";
  }
}
