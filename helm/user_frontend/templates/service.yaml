apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-user-frontend
  labels:
    app: {{ .Release.Name }}-user-frontend
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-user-frontend